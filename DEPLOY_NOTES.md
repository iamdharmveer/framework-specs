# Deploy: GAP-2026-08-28-MULTIPART-STEM-LAYOUT — release 2026.08.28.4

**Files to commit (7):** `Framework_MockTestCreate.md` (v5.79), `VERSION`,
`CHANGELOG.md`, `DEPLOY_NOTES.md`, `MANIFEST.json`, `SPEC_MANIFEST.json`,
`SPEC_SECTIONS.json`. One spec changed (Step 7 only): NEW §10 S10-2b multi-part stem
layout contract + S10-3 authoring helpers with hard raises, S10-1 format rule, S2-2
R-SUBPART, S4-11 item 44 (G-STEM-SUBPART). No engine changed. No other step changed —
Step 9 / Step 11 / PYQ flow are pass-through or out of scope by design. See CHANGELOG
2026.08.28.4.

**Operator change (all ~200 exams): NONE.** No project file, no exam_config, no
section_rules change anywhere. The rule is exam-agnostic: the label-collision test
renders each exam's OWN option labels at run time. FORWARD-ONLY: papers authored from
v5.79 onward carry the layout; already-delivered papers and all Row files keep theirs
(downstream fidelity gates make that permanent by design).

**Deploy order:** commit all 7 files together in one commit. bootstrap.py verifies
sha256 against MANIFEST.json at every session's Step 0 — a partial deploy (spec
without MANIFEST.json, or either manifest without the spec) HALTS every future
session at verification. The two manifests were regenerated by the repo's own
generators (gen_manifest.py, build_spec_manifest.py) after the spec edit.
